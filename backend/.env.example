# ============================================
# Server Configuration
# ============================================
NODE_ENV=development
# Set to 'production' for production deployments
# Options: development, staging, production

PORT=5000
# Port where API will run
# Change if 5000 is already in use

API_BASE_URL=http://localhost:5000
# URL where API is accessible from frontend
# Production: https://your-domain.com

# ============================================
# Database Configuration
# ============================================
DATABASE_URL=postgresql://user:password@localhost:5432/crypto_db
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Example: postgresql://postgres:password123@localhost:5432/crypto_db
# Production: Use managed database (AWS RDS, Railway, etc)

# ============================================
# JWT Configuration
# ============================================
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
# Secret key for signing JWT tokens
# IMPORTANT: Change this in production!
# Minimum 32 characters recommended
# Keep this SECRET - never commit to git

JWT_EXPIRATION=7d
# Token expiration time
# Options: 1d, 7d, 30d, 90d, 1y, etc

# ============================================
# External APIs
# ============================================
COINGECKO_API_URL=https://api.coingecko.com/api/v3
# CoinGecko API endpoint (free tier, no key required)
# Supports up to 10-50 calls/minute on free tier

COINGECKO_API_KEY=
# Optional: Your CoinGecko API key for higher rate limits
# Get from: https://www.coingecko.com/api/documentation

# ============================================
# CORS Configuration
# ============================================
CORS_ORIGIN=http://localhost:3000
# Frontend URL allowed to access this API
# Production: https://your-frontend-domain.com
# For multiple origins: http://localhost:3000,https://example.com

# ============================================
# Admin Credentials
# ============================================
ADMIN_EMAIL=admin@crypto.local
# Default admin email (used during seeding)

ADMIN_PASSWORD=admin123456
# Default admin password (change immediately in production!)
# Must be at least 8 characters with uppercase and number
